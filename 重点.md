该技术利用计算机模拟和机器学习算法，在实验之前对化合物库进行预筛选，大幅缩小实验范围，提高研发效率。虚拟筛选可分为两大类：

1. **基于结构的虚拟筛选（SBVS）**：利用靶点蛋白的三维结构进行分子对接
2. **基于配体的虚拟筛选（LBVS）**：基于已知活性化合物的结构特征进行相似性搜索

本项目采用基于配体的方法，利用深度学习技术从分子的化学结构中学习活性规律。

本项目聚焦两个核心预测任务：

- **BBBP（Blood-Brain Barrier Penetration）**：血脑屏障穿透性预测，属于二分类任务，对中枢神经系统药物研发至关重要
- **ESOL（Estimated SOLubility）**：水溶解度预测，属于回归任务，是药物成药性评估的关键指标

MoleculeNet等主流药物数据集

**解决过拟合问题**：针对药物数据集样本量小、特征维度高的特点，提出有效的正则化策略

本项目采用**ECFP4指纹**

SMILES转分子指纹、描述符计算，SMILES字符串 转成 1024维特征向量

用户输入SMILES
      ↓
┌─────────────────┐
│  SMILES解析     │  ← RDKit库
│  (字符串→分子)  │
└────────┬────────┘
         ↓
┌─────────────────┐
│  特征提取       │  ← Morgan指纹生成器
│  (分子→向量)    │
└────────┬────────┘
         ↓
┌─────────────────┐
│  模型推理       │  ← PyTorch神经网络
│  (向量→概率)    │
└────────┬────────┘
         ↓
┌─────────────────┐
│  后处理         │  ← Lipinski规则检查
│  (概率→结果)    │
└────────┬────────┘
         ↓
    输出预测结果